Setup:

1. Install node.js
   To verify installation run node -v in terminal
2. run npm install in terminal to install dependencies


Usage:

> npm start <input_file>

input_file - path to json file, if not specified default 'resources/data.json' will be used


Testing:

> npm test